<%= form_for @question do |f| %>

  <%= f.label :q, "Question" %>
  <%= f.text_field :q %>

  <%= f.submit 'Submit Changes' %>
<% end %>


<%= link_to 'Delete', question_path(@question), method: :delete %>